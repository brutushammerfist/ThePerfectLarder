#:import Window kivy.core.window.Window
<Login>:
	
	userName:userName                #for python code : for kivy code
	userPassword:userPassword        #for python code : for kivy code
	FloatLayout:
        Image:
            source: "graphics/loginbackground.png"
	GridLayout:
		cols:1
		size_hint: 1,.66
		padding: [60]
		spacing: [10,10]

		GridLayout:
			cols:1
				
		TextInput:
			height: 30
			id: userName
            hint_text: "username"
            max_lines: 1
            halign: 'center'
            font_size: 67
			multiline: False
			write_tab: False
				
		TextInput:
			height: 30
			id: userPassword
            hint_text: "password"
            max_lines: 1
            halign: 'center'
            font_size: 67
			multiline: False
			password: True
			write_tab: False
			
		Button:
			pos_hint:{"x":0.2,"y":0.05}
			font_size: 67
			halign: 'center'
			valign: 'center'
			text:"Login"
			#on_press: root.manager.current = 'homescreen' 
			on_press: root.userLogin()    #to check the login credentials
			
		GridLayout:
		Label:
			text: "Want your own Perfect Larder?"
			halign: 'center'
			font_size: 65
		Button:
			text: "Create an Account"
			halign: 'center'
			valign: 'center'
			font_size: 67
			on_press: 
				root.manager.transition.direction = 'left'
				root.manager.current ='signup'